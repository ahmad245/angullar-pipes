import { ConvertMKPipe } from './convert-m-k.pipe';

describe('ConvertMKPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMKPipe();
    expect(pipe).toBeTruthy();
  });
});
